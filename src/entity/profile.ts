import { Address } from "./address";

export class Profile {
    id: string
    username: string;
    firstName: string;
    lastName: string
    email: string
    age: string
    birthDate: string
    bloodGroup: string
    phone: string
    ssn: string
    image: string
    address: Address;
}